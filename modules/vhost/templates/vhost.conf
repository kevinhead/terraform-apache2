<VirtualHost *:443>
        ServerAdmin admin@${server_name}
        ServerName ${server_name}

        DocumentRoot ${document_root}

        ErrorLog ${error_log}
        CustomLog ${custom_log}
</VirtualHost>